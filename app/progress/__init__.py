"""Phase C — 조직 단위 공고 진행 상태 / 선점 패키지.

본 패키지는 task 00097 의 신규 테이블 ``announcement_progress`` /
``announcement_progress_history`` 를 다루는 repository / service / API 라우트를
모아둔다. 본 subtask (00097-3) 시점에는 repository 모듈만 존재하며, API 라우터와
UI 매크로는 후속 subtask 가 채운다.

설계 문서: ``docs/progress_org_design.md``.
"""
